[project]
name = "aerotunnel"
version = "0.2.3"
description = "TUI dashboard for monitoring and managing forward/reverse SSH tunnels for tunneled services"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "textual>=8.2.7",
]

[project.scripts]
aerotunnel = "aerotunnel:main"
